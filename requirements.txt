# ============================================================
# PecheurConnect v3.0 — Requirements
# Python 3.11 / Ubuntu Latest (GitHub Actions)
# ============================================================

# --- Core Async ---
aiohttp==3.9.5

# --- Variables d'environnement (tests locaux) ---
python-dotenv==1.0.1

# --- Données Marines & Copernicus ---
# Note : 1.3.3 est yanked (retirée par le mainteneur).
# On utilise >=1.3.4 pour prendre la première version stable suivante.
# Si vous connaissez la version exacte disponible sur votre machine,
# remplacez par copernicusmarine==X.Y.Z
copernicusmarine>=1.3.4

xarray==2024.2.0
zarr==2.17.1
numpy==1.26.4
pandas==2.2.1
