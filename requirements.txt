# ============================================================
# PecheurConnect v4.2 — Requirements
# Python 3.11 / Ubuntu Latest (GitHub Actions)
# Fix : zarr<3 pour compatibilité copernicusmarine 2.3.x
# ============================================================

# --- Core Async ---
aiohttp==3.9.5

# --- Variables d'environnement (tests locaux) ---
python-dotenv==1.0.1

# --- Données Marines & Copernicus ---
copernicusmarine==2.3.0

# --- Contrainte zarr : v3.x casse open_zarr() avec copernicusmarine ---
# zarr 2.x utilise l'ancienne API compatible avec copernicusmarine 2.3.x
zarr<3
numcodecs>=0.12,<1

# --- Traitement données ---
xarray==2024.2.0
numpy==1.26.4
pandas==2.2.1
