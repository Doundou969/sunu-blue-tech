name: PecheurConnect Update

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install copernicusmarine xarray netCDF4 numpy pandas requests python-dotenv
      
      - name: 🌊 Run script
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_ID: ${{ secrets.TG_ID }}
          COPERNICUS_USERNAME: ${{ secrets.COPERNICUS_USERNAME }}
          COPERNICUS_PASSWORD: ${{ secrets.COPERNICUS_PASSWORD }}
        run: python script_peche.py
      
      - name: ✅ Verify output
        run: |
          if [ -f data.json ]; then
            echo "✅ data.json créé avec $(jq length data.json) zones"
          else
            echo "❌ data.json manquant"
            exit 1
          fi
      
      - name: 💾 Commit
        run: |
          git config user.name "PecheurConnect Bot"
          git config user.email "bot@pecheurconnect.sn"
          git add data.json logs/ || true
          git diff --quiet && git diff --staged --quiet || git commit -m "🌊 $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push || echo "Rien à pusher"
