name: PecheurConnect-Runner

on:
  schedule:
    - cron: '0 */6 * * *'  # toutes les 6h
  workflow_dispatch:       # lancement manuel

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # 1️⃣ Checkout code
      - name: 📂 Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Setup Python
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # 3️⃣ Install dependencies
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4️⃣ Run PecheurConnect script
      - name: 🚀 Run script_peche.py
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_ID: ${{ secrets.TG_ID }}
          COPERNICUS_USERNAME: ${{ secrets.COPERNICUS_USERNAME }}
          COPERNICUS_PASSWORD: ${{ secrets.COPERNICUS_PASSWORD }}
        run: python script_peche.py

      # 5️⃣ Commit & push data.json si changé
      - name: 🌐 Commit & Push data.json
        run: |
          git config --global user.name "PecheurConnect-Bot"
          git config --global user.email "bot@pecheurconnect.sn"
          git add data.json
          CHANGES=$(git status --porcelain)
          echo "Changements détectés : $CHANGES"
          if [ -n "$CHANGES" ]; then
            git commit -m "📊 Mise à jour data.json $(date +'%d-%m-%Y %H:%M')"
            git push origin main
          else
            echo "✅ Aucun changement détecté. Pas de commit nécessaire."

      # 6️⃣ Publish Leaflet map to GitHub Pages
      - name: 🌊 Publish map to GitHub Pages
        uses: peaceiris/actions-gh-pages@v5.1.23
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./templates
          publish_branch: gh-pages
