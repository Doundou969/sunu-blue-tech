name: PecheurConnect Auto-Tracker

on:
  schedule:
    - cron: '0 0,12 * * *'
  workflow_dispatch:

jobs:
  run-tracker:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📂 Checkout du code
        uses: actions/checkout@v3

      - name: 🐍 Installation de Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11' # Changé de 3.10 à 3.11

      - name: 📦 Installation des dépendances
        run: |
          python -m pip install --upgrade pip
          pip install numpy matplotlib requests copernicusmarine xarray netCDF4 dask

      - name: 🚀 Exécution du script PecheurConnect
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_ID: ${{ secrets.TG_ID }}
          COPERNICUS_USERNAME: ${{ secrets.COPERNICUS_USERNAME }}
          COPERNICUS_PASSWORD: ${{ secrets.COPERNICUS_PASSWORD }}
        run: python script_peche.py

      - name: 📊 Archivage de l'historique CSV
        run: |
          git config --global user.name "PecheurConnect-Bot"
          git config --global user.email "bot@pecheurconnect.sn"
          git add pecheur_coast_history.csv
          git commit -m "📊 Mise à jour historique côte Sénégal [skip ci]" || echo "Aucune modification"
          git push
