name: Rapport quotidien pêche 🇸🇳

on:
  schedule:
    - cron: '0 8 * * *'  # 8h UTC = 9h Dakar
  workflow_dispatch:  # Bouton manuel

jobs:
  peche:
    runs-on: ubuntu-latest
    name: 🚀 Lancer le script de pêche (debug)
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Configurer Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Installer dépendances
      run: |
        pip install requests pandas beautifulsoup4 python-telegram-bot==20.7
        
    - name: Lancer script pêche
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: python script_peche.py
